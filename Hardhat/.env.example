# Optional: hardhat.config.ts falls back to this same public endpoint.
TESTNET_RPC_URL=https://rpc.testnet.whitechain.io

# Throwaway deployer key, 32 bytes hex, with or without 0x. Needed only to
# deploy or verify. Fund it at https://faucet.testnet.whitechain.io
PRIVATE_KEY=
